#!/bin/bash -e

# make directory for working

DIR=$HOME/fdos_dir/
if test -f "$DIR"; then
        echo "$DIR exists."
else 
        echo "$DIR does not exist."
        mkdir -p fdos_dir = $HOME/freedos

fi

mkdir -p fdos_dir = $HOME/freedos

